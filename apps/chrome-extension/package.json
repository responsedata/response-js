{
  "name": "@responsedata/chrome-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chrome DevTools inspector for Response browser analytics",
  "scripts": {
    "build": "node scripts/build.mjs && tsc --project tsconfig.json",
    "test": "node --test test/*.test.js"
  }
}
